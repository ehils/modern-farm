// define a harvestPlants fucntion
// accept plants array as input
// return an array of seed objects
// iterate array of growing plants
// on each plant get the value of the output property

const harvestPlants = (plantArr) => {
    let seedObj = 
    return seedObj
}